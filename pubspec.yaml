name: drivecare
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # ✅ UPDATED (works with your Flutter 3.41)
  google_fonts: ^6.2.1

  provider: ^6.1.1
  intl: ^0.19.0

  # ✅ Latest stable Firebase (compatible with your setup)
  firebase_core: ^3.13.0
  firebase_auth: ^5.3.3
  cloud_firestore: ^5.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true